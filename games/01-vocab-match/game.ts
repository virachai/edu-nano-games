/**
 * Game 1 — Vocab Match (placeholder scaffold).
 * DOM game: match words to translations. Full implementation lands in Phase 1;
 * pure matching/scoring logic will live here and be covered by
 * tests/vocab-match.test.js per the plan.
 */
export const GAME_ID = "vocab-match" as const;
