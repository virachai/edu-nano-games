# Evidence — WS-001 Workspace Build / Typecheck / Test Gates

## Result

WS-001 complete. Both `packages/*` packages are integrated into the workspace project-reference graph and are exercised by the workspace build, typecheck, and test gates. All gates pass. Status: **DONE**.

## Task / Status

- Task: WS-001 — Integrate `packages/*` into workspace build/typecheck/test gates (`docs/tasks/WS-001.md`)
- Status: DONE (implementation + verification + evidence complete)
- Date verified: 2026-09-17

## Packages Covered

| Package                            | Integration mode                                                                                                                                                       |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@edu-nano-games/semantic-engine`  | New `packages/semantic-engine/tsconfig.json`; typechecked from its JS sources via `allowJs` (`noEmit`; emitted outputs gitignored)                                     |
| `@edu-nano-games/remotion-runtime` | New `packages/remotion-runtime/tsconfig.json`; typechecked from `src/*.jsx` with `jsx: react-jsx` (`noEmit`); React/Remotion types resolve from installed dependencies |
| games 01/02/03, `shared/`          | Pre-existing references, unchanged                                                                                                                                     |

Root `tsconfig.json` now references 6 projects: 3 games, `shared`, `packages/semantic-engine`, `packages/remotion-runtime`. `pnpm-workspace.yaml` already includes `packages/*`.

## Configuration Changes

- `tsconfig.json` — added project references to `packages/semantic-engine` and `packages/remotion-runtime`.
- `tsconfig.base.json` — added `allowJs: true` (semantic-engine sources are plain JS).
- `packages/semantic-engine/tsconfig.json` (new) — package project config, `noEmit`.
- `packages/remotion-runtime/tsconfig.json` (new) — package project config, `noEmit`, JSX support.
- `.gitignore` — ignore emitted artifacts: `packages/semantic-engine/*.{js,js.map,d.ts,d.ts.map}` and `packages/*/tsconfig.tsbuildinfo`.
- `eslint.config.js` — repaired gate health: `ecmaVersion` 2024 → 2025 (tests use import attributes `with { type: "json" }`), added `globals.node` (tests use `structuredClone`); restored the explanatory header comment.
- `docs/09-runbook/09-01-workspace-verification-runbook.md` (new) — documents the verification contract.

## Commands Executed & Results

Environment: Windows (bash), Node v25.0.0, pnpm 12.4.2, TypeScript 7.0.2.

| #   | Command                       | Result                                                                       |
| --- | ----------------------------- | ---------------------------------------------------------------------------- |
| 1   | `pnpm build`                  | **PASS** (exit 0; `tsc -b` across all 6 project references)                  |
| 2   | `npx tsc -b --force`          | **PASS** (exit 0; clean full typecheck, caches rebuilt — not a cached no-op) |
| 3   | `pnpm typecheck`              | **PASS** (exit 0)                                                            |
| 4   | `pnpm test`                   | **PASS** (exit 0; `tsc -b` then `node --test`; 33/33 tests)                  |
| 5   | `node --test tests/*.test.js` | **PASS** (tests 33, pass 33, fail 0)                                         |
| 6   | `pnpm lint`                   | **PASS** (exit 0, after the eslint config repair described above)            |

## Regression Verification

- The 33 passing tests include the MM-001 suites (`tests/math-motion-contract.test.js`, `tests/math-motion-semantic-execution.test.js`) — no MM-001 regression.
- Baseline comparison via `git stash` before finalizing: the lint parse errors (`ecmaVersion`) and `no-undef` (`structuredClone`) failures pre-existed WS-001 and were repaired in scope; `format:check` failures also pre-existed.
- Game and shared tsconfigs were not modified; base config change is additive (`allowJs`).

## Known Limitations

- `pnpm format:check` fails on 30 pre-existing unformatted files (baseline before this changeset: 49). Repo-wide formatting is out of WS-001 scope; all files touched by this task are prettier-formatted.
- `semantic-engine` is typechecked from its JS sources (no `.ts` sources exist in-repo), so type coverage is limited by JSDoc/inference.
- `remotion-runtime` typecheck depends on `react`/`remotion`/`@remotion/cli` being installed in `node_modules` (present in this environment).
- ESLint intentionally does not use typescript-eslint (per pre-existing config note); `.ts` sources are covered by `tsc` only.

## Commit Hashes

- Implementation commit: `b15fad8`
- Evidence commit: the commit immediately following the implementation commit (this file was updated with the implementation hash in that commit).
