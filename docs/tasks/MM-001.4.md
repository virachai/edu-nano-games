# MM-001.4 — Remotion Compilation

## Objective
Compile the validated 6174 semantic trace into deterministic, timeline-valid Remotion scene descriptors.

## Scope
- Semantic-trace-to-scene compilation.
- Scene timing, ordering, identity, and duration validation.
- Deterministic compilation replay.

## Dependencies
MM-001.3 — DONE.

## Inputs / Files to Read
- `docs/02-delivery/02-01-work-breakdown.md`
- `packages/semantic-engine/remotion-compiler.js`
- `packages/semantic-engine/validation.js`
- `packages/remotion-runtime/`
- `tests/math-motion-contract.test.js`

## Agent Instructions
Compile only validated semantic traces through the existing compiler boundary. Preserve deterministic scene descriptors and contiguous timeline semantics.

## Constraints
Do not bypass semantic validation or hand-edit generated scene descriptors. Avoid unrelated renderer changes.

## Acceptance Criteria
- Validated semantic trace compiles to valid scene descriptors.
- Compilation is deterministic.
- Scene timing and duration are consistent.
- Compiled terminal state matches semantic execution terminal state.

## Verification
Run the Remotion compilation and math-motion contract tests.

## Evidence Required
Record commands, test results, scene descriptor/artifact paths, and any limitations.

## Completion
DONE only when acceptance criteria and verification are supported by repository evidence.
