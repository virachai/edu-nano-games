# MM-001.5 — Real Render

## Objective

Complete and verify a real Remotion render of the canonical `math-motion/concepts/6174` vertical slice from repository source.

## Scope

- Use the existing canonical 6174 concept/recipe and Remotion runtime/compiler path.
- Execute the repository's documented real-render path.
- Produce an actual render artifact when the environment permits it.
- Verify source-to-recipe-to-render traceability and inspect the resulting artifact.
- Record reproducible failure details if the environment prevents a real render.

## Dependencies

- MM-001.4 — Remotion Compilation — DONE.

## Inputs / Files to Read

- `docs/AGENT_RULES.md`
- `docs/BACKLOG.md`
- `INDEX.md`
- `FLOW.md`
- `docs/02-delivery/02-01-work-breakdown.md`
- `docs/11-math-motion/11-01-architecture-adoption.md`
- `math-motion/concepts/6174/`
- `packages/semantic-engine/`
- `packages/remotion-runtime/` (if present)

## Agent Instructions

1. Inspect the repository's package scripts and existing render tooling before running commands.
2. Use the canonical recipe path; do not hand-edit generated render descriptors or artifacts.
3. Attempt the documented real render.
4. Verify the produced artifact, or capture the exact reproducible environment/dependency failure if rendering cannot complete.
5. Do not expand scope into unrelated renderer or architecture changes.

## Constraints

- Preserve the canonical recipe as the single source of truth.
- Do not mark this task `DONE` without an actual render artifact and required verification evidence.
- Do not conceal environment or dependency failures.
- Do not delete unrelated artifacts or user work.

## Acceptance Criteria

- A real Remotion render completes from repository source, or a reproducible blocker is captured if it cannot complete.
- The render input is traceable to the canonical 6174 concept and recipe.
- The rendered artifact is inspected for successful creation and basic validity.
- Existing semantic/contract verification remains passing.

## Verification

Run the exact render command documented by the repository/package scripts and the relevant existing semantic/contract tests. Record command output and artifact path in evidence.

## Evidence Required

`docs/12-evidence/MM-001.5.md` containing:

- task ID and final state
- commands executed
- render result
- artifact path and traceability information
- verification results
- environment limitations or risks

## Completion

Move through the backlog state machine only when the evidence supports the state. If a real render is impossible because of the environment, use `BLOCKED` and preserve the failure evidence rather than claiming completion.
