# WS-003 — Agent State Transition Validation

**Status:** READY  
**Priority:** P1  
**Depends On:** —  
**Owner:** Local Agent  
**Created:** 2026-09-18  
**Updated:** 2026-09-18  
**Evidence:** `docs/12-evidence/WS-003.md`

## Objective

Close the WS-002 execution lifecycle correctly by recording its completed evidence state in the backlog, then add one small automated regression test proving the state/evidence contract.

## Scope

### In Scope

- Update the WS-002 backlog state from `READY` to `EVIDENCE_COMPLETE`.
- Update `docs/12-evidence/WS-002.md` so its changed-files/evidence record is complete and truthful.
- Add `tests/agent-state-transition-validation.test.js`.
- The test must read `docs/BACKLOG.md` and `docs/12-evidence/WS-002.md` at runtime.
- Assert that WS-002 is `EVIDENCE_COMPLETE` and that its evidence references the implementation test and the evidence file.

### Out of Scope

- Marking WS-002 `DONE`.
- Changing `docs/AGENT_RUNBOOK.md`.
- Changing application/runtime code.
- Refactoring existing tests.
- Creating an agent loop or process runner.
- Unrelated documentation changes.

## Preconditions

- [ ] WS-002 has a valid VERIFIED handoff.
- [ ] `docs/tasks/WS-002.md` exists.
- [ ] `docs/12-evidence/WS-002.md` exists.
- [ ] `docs/BACKLOG.md` contains WS-002.
- [ ] Node.js test runner is available.

If a precondition fails, stop and report the exact blocker.

## Inputs / Read First

- `docs/AGENT_RULES.md`
- `docs/AGENT_RUNBOOK.md`
- `docs/BACKLOG.md`
- `docs/tasks/WS-002.md`
- `docs/12-evidence/WS-002.md`
- `docs/12-evidence/12-01-evidence-template.md`
- `package.json`
- `tests/agent-protocol-validation.test.js`

## Procedure

1. Confirm WS-002's reported verification and evidence.
2. Update only the WS-002 row in `docs/BACKLOG.md` from `READY` to `EVIDENCE_COMPLETE`.
3. Correct WS-002 evidence so changed files and artifacts accurately include the evidence file itself.
4. Add `tests/agent-state-transition-validation.test.js` using existing Node test conventions.
5. Run `pnpm test`.
6. Create `docs/12-evidence/WS-003.md` with actual commands/results and acceptance-criteria evidence.
7. Return the standard handoff from `docs/AGENT_RUNBOOK.md`.
8. Stop. Do not mark WS-002 `DONE`; reviewer approval remains with DWB105.

## Constraints

- Keep the implementation minimal and dependency-free.
- Use only Node built-ins and the existing test runner.
- Do not modify `docs/AGENT_RUNBOOK.md` to make the test pass.
- Do not alter the WS-002 implementation test except when necessary to preserve existing behavior.
- Do not mark reviewer approval yourself.
- Do not change any other backlog task state.

## Acceptance Criteria

- [ ] WS-002 is `EVIDENCE_COMPLETE` in `docs/BACKLOG.md`.
- [ ] WS-002 evidence truthfully lists the implementation test and evidence artifact.
- [ ] `tests/agent-state-transition-validation.test.js` exists.
- [ ] The new test reads the backlog and WS-002 evidence at runtime.
- [ ] The test verifies the WS-002 state and required evidence references.
- [ ] `pnpm test` passes.
- [ ] WS-002 remains not-`DONE`; DWB105 review is still required.

## Verification

Run:

```bash
pnpm test
```

Expected result:

- Command exits successfully.
- Existing tests remain green.
- Both agent protocol validation tests pass.

## Evidence Required

Create `docs/12-evidence/WS-003.md` containing:

- task/status
- changed files
- exact commands executed
- verification results
- acceptance-criteria mapping
- known limitations/risks
- reproduction steps
- blocker record if applicable

## Definition of Done

The agent may report completion only when:

- implementation is complete
- all acceptance criteria pass
- `pnpm test` passes
- WS-002 is `EVIDENCE_COMPLETE`
- WS-002 is not marked `DONE`
- WS-003 evidence is complete and truthful
- no unresolved in-scope blocker remains

Local-agent completion is not reviewer approval.

## Final Handoff

```text
TASK: WS-003
STATUS: <IMPLEMENTED|VERIFIED|EVIDENCE_COMPLETE|DONE|BLOCKED>
SUMMARY: <short factual summary>
CHANGED:
- <file>
VERIFICATION:
- pnpm test — PASS/FAIL
EVIDENCE:
- docs/12-evidence/WS-003.md
BLOCKERS:
- <none or exact blocker>
FOLLOW-UP:
- <none or task-id/recommended action>
```
